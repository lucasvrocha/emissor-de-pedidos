import { StopEventDirective } from './stop-event.directive';

describe('StopEventDirective', () => {
  it('should create an instance', () => {
    const directive = new StopEventDirective();
    expect(directive).toBeTruthy();
  });
});
